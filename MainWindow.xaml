<Window
    x:Class="PhotoRenameAIHash.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PhotoRenameAIHash"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Photo Rename AI · Hash">

    <Grid Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">
        <NavigationView x:Name="NavView"
                        SelectionChanged="NavView_SelectionChanged"
                        Header="{x:Bind ViewModel.AppTitle, Mode=OneWay}"
                        IsBackButtonVisible="Collapsed"
                        PaneBackground="{ThemeResource SolidBackgroundFillColorSecondaryBrush}">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="整理" Tag="Organize" Icon="Pictures" IsSelected="True" />
                <NavigationViewItem Content="去重" Tag="Deduplicate" Icon="Map" />
                <NavigationViewItem Content="设置" Tag="Settings" Icon="Setting" />
            </NavigationView.MenuItems>
            <NavigationView.FooterMenuItems>
                <NavigationViewItem Content="关于" Tag="About" Icon="Help" />
            </NavigationView.FooterMenuItems>
            <Frame x:Name="ContentFrame" />
        </NavigationView>
    </Grid>
</Window>
